import 'package:flutter/material.dart';
import 'package:flutter_practice/widgets/appBar.dart';

class SeventhSecreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: appBar(title: 'For Theme',),
      body: Column(
        children: [

        ],
      ),
    );
  }

}